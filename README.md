# Doublon
Remove duplicate lines in a file. Warning, it will erase the previous file.

## Usage

```
Doublon
Dedup lines in file.

Usage:
  doublon <file>
  doublon (-h | --help)

Options:
  -h --help     Show this screen.
```
